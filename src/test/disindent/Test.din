(package me::enerccio::pack)
(native qux::bar)

(use test)
(include * from bar)
(include (xbar xbaz xfoo) from qux)

(det fncf
	x->int
	y->int
	(k->long (+ x y)))

(def foo->fncf [a->test::type b->(int <- long int)]
	(-> (anca <-)
		(b a:x a:k (foo2 a):y))) 

(def foo1->int [] 5)

(defn foo2->fncb [m->bool])

(def foo3->double [a->int b->int]
	(fnc->long [x->double z->long]
		(if (= x 0)
			(for ((avg q->int) 0 (< q a) (+ q 2))
					(bar z b)
					(->int 
						(* x q))))))
	