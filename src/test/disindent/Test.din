use Mara.mara
use es.me.ral.da.Anca.anca
use Wat.wat

define test with XXX, YYY
	foo as int
	bar as float
	baz as string[]
	qux as any[][]
	aaa as XXX
	bbb as YYY
	ccc as mara<XXX>
	cc as mara<int>
	ddd as anca
	edd as anca<XXX, YYY>[]
	
define wat
	x as wat
	
	
int anca(mara is int, jozef is string)
    somefunc 
        someotherfunc(mara, jozef)
        somethingelse()
        jozef 
